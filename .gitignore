# macOS
.DS_Store

# logs / temporaries
*.log
*.tmp

# packaging output
*.zip
*.csv
node_modules/
dist/
*.crx

# local-only test values (never commit, never push - see verify.sh stage 7)
tools/local_only.txt
